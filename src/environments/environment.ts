// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCPzSeCdoIV64CSCTtEZkFMf_TyrXii06g",
    authDomain: "gui-zapata.firebaseapp.com",
    databaseURL: "https://gui-zapata.firebaseio.com",
    projectId: "gui-zapata",
    storageBucket: "gui-zapata.appspot.com",
    messagingSenderId: "421010548190",
    appId: "1:421010548190:web:9121b43a68bd9ffd18294f"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
